server:
  port: 3344
spring:
  application:
    name: springcloud-config-server
  # 连接远程仓库
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Venns-Git/Venns-study.git # 远程git仓库地址 只能是https的。